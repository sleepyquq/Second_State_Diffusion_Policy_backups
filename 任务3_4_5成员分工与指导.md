# 任务3/4/5 成员分工与具体指导

本文档面向后续接手任务三、任务四、任务五的成员。当前任务2已经完成基础训练与交付，但需要明确：

- `PickCube-v1` 已稳定收敛，可作为抓取任务 sanity check 和稳定 baseline。
- `PegInsertionSide-v1` 已跑通并有成功视频，但尚未稳定收敛。当前 30 episode 确认成功率约 30%，后续应把 Peg 作为主要攻关对象。

如果原提案中任务三、四、五的命名与本文略有差异，以提案标题为准；本文给出的是基于当前项目状态的可执行分工和验收标准。

## 0. 与原提案任务的对齐说明

原提案中的后续任务分工应按下面方式理解：

| 原提案任务 | 成员主责 | 当前 Peg 未稳定收敛时是否能继续 | 调整后的工作口径 |
|---|---|---|---|
| 任务3 | 数据规模实验 | 可以继续，而且应优先继续 | 重点验证 Peg 成功率是否受高质量 motionplanning 数据规模限制 |
| 任务4 | 视觉扰动实验 | 可以开始，但不能直接给出 Peg 视觉鲁棒性最终结论 | 先搭视觉扰动 pipeline；当前 state policy 不吃图像，视觉扰动不会进入策略输入 |
| 任务5 | 最终整合与交付 | 可以同步准备 | 负责整合任务2 baseline、任务3数据规模结果、任务4扰动结果 |

当前 Peg 没有稳定收敛，并不阻止任务3/任务4开展，但会改变结论边界：

1. 任务3的数据规模实验仍然有效，因为它正好回答“Peg 不稳定是否由高质量数据量不足导致”。
2. 任务4的视觉扰动实验如果继续使用当前 `state` policy，只能验证扰动场景、渲染和录制流程，不能证明真正的视觉输入鲁棒性。
3. 如果任务4要形成正式视觉扰动鲁棒性结论，需要训练 `rgbd` 或其他图像输入策略；当前任务2交付的是 `state` policy。

## 1. 当前可继承资产

### 1.1 可直接使用的模型

| 任务 | 模型 | 状态 |
|---|---|---|
| PickCube | `output_pickcube_state_final/checkpoints/policy_best.pt` | 稳定收敛，10 ep 100% |
| PegInsertionSide | `output_peg_state_motionplanning_resume/checkpoints/policy_best.pt` | 可运行 baseline，30 ep 30%，未稳定收敛 |

### 1.2 可直接使用的数据

| 任务 | 数据路径 | 建议用途 |
|---|---|---|
| PickCube | `official_demos/PickCube-v1/motionplanning/trajectory.state.pd_joint_delta_pos.physx_cpu.h5` | sanity check、回归测试 |
| Peg motionplanning | `official_demos/PegInsertionSide-v1/motionplanning/trajectory.state.pd_joint_delta_pos.physx_cpu.h5` | Peg 主训练数据 |
| Peg RL | `official_demos/PegInsertionSide-v1/rl/trajectory.state.pd_joint_delta_pos.physx_cpu.h5` | 仅作分析或筛选，不建议直接混入 |
| Peg combined | `official_demos/PegInsertionSide-v1/combined/trajectory.state.pd_joint_delta_pos.physx_cpu.h5` | 已验证效果差，不建议作为默认数据 |

### 1.3 基准评估命令

PickCube：

```powershell
$env:KMP_DUPLICATE_LIB_OK='TRUE'
$env:GIT_PYTHON_REFRESH='quiet'
& 'C:\micromamba\Library\bin\micromamba.exe' run -p 'D:\Second_State_Diffusion_Policy\.mambaenv' python evaluate_checkpoint.py `
  --checkpoint output_pickcube_state_final/checkpoints/policy_best.pt `
  --env PickCube-v1 `
  --episodes 10 `
  --max-steps 100 `
  --action-horizon 8 `
  --inference-steps 20
```

Peg：

```powershell
$env:KMP_DUPLICATE_LIB_OK='TRUE'
$env:GIT_PYTHON_REFRESH='quiet'
& 'C:\micromamba\Library\bin\micromamba.exe' run -p 'D:\Second_State_Diffusion_Policy\.mambaenv' python evaluate_checkpoint.py `
  --checkpoint output_peg_state_motionplanning_resume/checkpoints/policy_best.pt `
  --env PegInsertionSide-v1 `
  --episodes 30 `
  --max-steps 350 `
  --action-horizon 10 `
  --inference-steps 20 `
  --csv-out output_peg_state_motionplanning_resume/eval_confirm_best_ah10_max350.csv
```

## 2. 任务三：数据规模实验

### 2.1 目标

任务三按原提案负责数据规模实验。当前 Peg baseline 已能成功插入，但 30 episode 成功率只有 30%，尚未稳定收敛。因此任务三的核心问题不是“随便改模型把结果做高”，而是系统回答：

> Peg 插入任务不稳定，是否主要由高质量示范数据规模不足导致？

任务三可以继续进行，而且是当前 Peg 最需要的下一步工作。所有实验应尽量保持模型结构、训练参数、评估参数一致，只改变数据规模或数据质量，否则无法判断性能变化是否来自数据规模。

建议目标：

| 等级 | 验收标准 |
|---|---|
| 最低合格 | 30 episode 成功率超过当前 baseline 30%，且保留完整证据 |
| 推荐目标 | 30 episode 成功率达到 50% 以上 |
| 优秀目标 | 50 episode 成功率达到 60% 以上，并有多 seed 视频 |

### 2.2 数据规模设计

推荐至少做以下数据规模：

| 数据规模 | 数据来源 | 目的 |
|---:|---|---|
| 50 | Peg motionplanning 成功轨迹子集 | 小数据下限 |
| 100 | Peg motionplanning 成功轨迹子集 | 与 PickCube 数据量对齐 |
| 165 | 当前完整 Peg motionplanning 数据 | 任务2 baseline |
| 300 | 新增 Peg motionplanning 成功轨迹 | 判断扩数据是否明显改善 |
| 500 | 新增 Peg motionplanning 成功轨迹 | 判断是否进入稳定收敛区间 |

必须注意：

- 数据规模实验应优先使用同分布、高质量 `motionplanning` 成功 replay 轨迹。
- 不要把当前 `combined` 数据直接当作“更多数据”的主结论，因为 motionplanning + RL replay 混合已经验证效果变差。
- 如果使用 RL 数据，必须单独作为“数据来源/数据质量实验”，不要和纯数据规模实验混在一起。

### 2.3 任务三输入

优先使用：

```text
official_demos/PegInsertionSide-v1/motionplanning/trajectory.state.pd_joint_delta_pos.physx_cpu.h5
output_peg_state_motionplanning_resume/checkpoints/policy_best.pt
output_peg_state_motionplanning_resume/metrics.csv
```

不要直接使用 combined 数据作为默认训练集，因为当前已经验证它会降低效果。

### 2.4 推荐实验方向

#### 方向 A：增加高质量 motionplanning replay 数据

当前 motionplanning replay 只有 165 条。Peg 插入需要更多末端对准覆盖，建议补到 300-500 条高质量成功轨迹。

要求：

- 只保留 replay 成功轨迹。
- 保持 `obs_mode=state`、`control_mode=pd_joint_delta_pos`、`physx_cpu`。
- 记录 replay 成功率和最终轨迹数。

验收文件：

```text
official_demos/PegInsertionSide-v1/motionplanning/trajectory.state.pd_joint_delta_pos.physx_cpu.h5
logs/peg_replay_more_motionplanning.log
```

#### 方向 B：保持模型不变的数据规模对照

任务三的第一优先级是控制变量。建议先保持当前 `train_dp.py` 的模型结构不变，只改变数据规模：

- 50 条
- 100 条
- 165 条
- 300 条
- 500 条

统一评估：

- `episodes=30`
- `max_steps=350`
- `action_horizon=10`
- `inference_steps=20`

只有当数据规模增加后仍不能提升，才建议把“模型结构不足”作为任务三的附加分析。

#### 方向 C：可选的模型结构对照

当前 `train_dp.py` 使用的是轻量条件网络。如果任务三完成数据规模主实验后还有时间，可以实现更接近标准 Diffusion Policy 的 1D UNet，例如：

- action 序列使用 Conv1D / ResBlock 结构。
- timestep embedding 通过 FiLM 或 AdaGN 调制。
- obs condition 使用更强 MLP encoder。
- 保留当前 `evaluate_policy` 和 `record_policy_video.py`，保证评估口径一致。

注意：

- 改结构前先保证 PickCube 仍能快速达到高成功率。
- 不要只看 training loss，必须看 30 episode eval。

#### 方向 D：多候选采样与动作选择

Peg 末端插入对随机采样敏感。可以在评估/推理阶段一次生成多个候选动作序列，再选择更平滑或更接近专家分布的序列。

可尝试：

- 每步采样 `K=4/8` 条 action sequence。
- 选择动作幅度更小、变化更平滑的序列。
- 使用最近观测与 demo 中相似状态的动作统计作为选择约束。

注意：如果加入选择器，必须和原 baseline 分开命名输出目录，避免混淆。

#### 方向 E：末端阶段专门优化

失败 episode 经常 reward 很高，说明策略接近成功但插入深度或姿态不稳。建议分析失败视频/状态：

- 统计成功与失败 episode 最后 50 步动作分布。
- 检查动作是否频繁饱和到 `[-1, 1]`。
- 检查是否在 peg 接近孔位后仍出现大幅横向动作。
- 可尝试降低末端动作幅度，例如对最后执行动作乘以 0.5-0.8，但必须用评估验证。

### 2.5 推荐训练命令模板

从当前 Peg baseline 继续训练：

```powershell
& 'C:\micromamba\Library\bin\micromamba.exe' run -p 'D:\Second_State_Diffusion_Policy\.mambaenv' python train_dp.py `
  --data official_demos/PegInsertionSide-v1/motionplanning/trajectory.state.pd_joint_delta_pos.physx_cpu.h5 `
  --env PegInsertionSide-v1 `
  --obs-mode state `
  --epochs 200 `
  --batch-size 256 `
  --lr 2e-5 `
  --weight-decay 1e-6 `
  --ema-decay 0.995 `
  --eval-every 20 `
  --eval-episodes 10 `
  --eval-max-steps 350 `
  --action-horizon 10 `
  --output-dir output_peg_task3_experiment_<name> `
  --num-timesteps 100 `
  --inference-steps 20 `
  --resume output_peg_state_motionplanning_resume/checkpoints/policy_best.pt
```

从头训练新结构时：

```powershell
& 'C:\micromamba\Library\bin\micromamba.exe' run -p 'D:\Second_State_Diffusion_Policy\.mambaenv' python train_dp.py `
  --data official_demos/PegInsertionSide-v1/motionplanning/trajectory.state.pd_joint_delta_pos.physx_cpu.h5 `
  --env PegInsertionSide-v1 `
  --obs-mode state `
  --epochs 400 `
  --batch-size 256 `
  --lr 1e-4 `
  --weight-decay 1e-6 `
  --ema-decay 0.995 `
  --eval-every 25 `
  --eval-episodes 10 `
  --eval-max-steps 350 `
  --action-horizon 10 `
  --output-dir output_peg_task3_unet_<name> `
  --num-timesteps 100 `
  --inference-steps 20
```

### 2.6 任务三交付清单

必须交付：

```text
output_peg_task3_<name>/metrics.csv
output_peg_task3_<name>/training_curves.png
output_peg_task3_<name>/checkpoints/policy_best.pt
output_peg_task3_<name>/checkpoints/policy_last.pt
output_peg_task3_<name>/eval_confirm_30ep.csv
output_peg_task3_<name>/videos/peg_task3_success.mp4
logs/peg_task3_<name>.log
```

必须写清：

- 使用的数据是哪一个。
- 数据规模是多少条。
- 数据是否为纯 motionplanning 成功轨迹。
- 是否 resume，resume 自哪个 checkpoint。
- 与当前 baseline 的成功率对比。
- 是否仍能录制成功视频。
- 如果失败，说明失败原因和下一步建议。

任务三最终建议输出一张表：

| 数据规模 | 数据来源 | episodes | success | mean reward | mean steps | checkpoint | 视频 |
|---:|---|---:|---:|---:|---:|---|---|
| 50 | motionplanning | 30 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
| 100 | motionplanning | 30 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
| 165 | motionplanning | 30 | 30% baseline | 99.19 | 290.4 | `output_peg_state_motionplanning_resume/checkpoints/policy_best.pt` | `output_peg_state_motionplanning_resume/videos/peg_baseline.mp4` |
| 300 | motionplanning | 30 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
| 500 | motionplanning | 30 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |

## 3. 任务四：视觉扰动实验

### 3.1 目标

任务四按原提案负责视觉扰动实验。当前需要特别说明：任务2交付的 PickCube 和 Peg 模型都是 `state` policy，输入是状态向量，不是 RGB/RGBD 图像。因此相机、光照、纹理、背景等视觉扰动不会直接进入当前策略输入。

这意味着任务四可以开始，但实验结论必须分层：

1. 如果继续使用当前 `state` policy，视觉扰动实验只能验证环境扰动、渲染、视频录制和评估流程，不能证明策略具有视觉鲁棒性。
2. 如果要得出正式视觉扰动鲁棒性结论，任务四需要训练或接入 `rgbd` / image-based policy。
3. Peg 当前没有稳定收敛，所以 Peg 上的视觉扰动结果只能作为预实验；正式扰动主结果建议先用已稳定收敛的 PickCube。

建议目标：

1. 搭建视觉扰动 pipeline，包括相机、光照、背景、颜色/纹理扰动和视频录制。
2. 先用 PickCube 稳定模型验证 pipeline。
3. 在 Peg 上做预实验，但报告中明确 Peg baseline 未稳定收敛。
4. 如果要完成真正视觉鲁棒性实验，需要训练 RGBD policy 或其他视觉输入模型。

### 3.2 当前 baseline

| 任务 | checkpoint | 标准评估 |
|---|---|---|
| PickCube | `output_pickcube_state_final/checkpoints/policy_best.pt` | `episodes=10, max_steps=100, action_horizon=8` |
| Peg | `output_peg_state_motionplanning_resume/checkpoints/policy_best.pt` | `episodes=30, max_steps=350, action_horizon=10` |

### 3.3 任务四可行路线

#### 路线 A：先用 PickCube 做视觉扰动主实验

PickCube 已经稳定收敛，适合用来搭建视觉扰动流程。

可以做：

- 改变光照强度和方向。
- 改变相机位置或视角。
- 改变背景颜色/纹理。
- 改变物体颜色。
- 录制扰动前后视频。

但如果仍使用当前 `state` policy，报告必须写清：

> 视觉扰动仅影响渲染画面，不影响 state policy 的策略输入，因此该实验验证的是扰动环境和可视化流程，不代表视觉输入策略的鲁棒性。

#### 路线 B：Peg 上做预实验

Peg 可以做视觉扰动预实验，但结论要降级：

- 可以验证扰动场景能否正常运行。
- 可以录制 Peg 成功/失败视频。
- 可以观察扰动下的可视化效果。
- 不能把当前结果写成“Peg 视觉鲁棒性已验证”。

原因：

1. Peg baseline 本身未稳定收敛。
2. 当前 Peg policy 是 `state` 输入。
3. 视觉扰动不会进入策略输入。

#### 路线 C：训练 RGBD policy 作为正式视觉扰动对象

如果任务四要严格完成原提案的“视觉扰动鲁棒性”，需要新增视觉策略训练：

- 实现 RGBD/RGB observation loader。
- 实现 image encoder。
- 保存视觉 demo 数据。
- 在无扰动视觉条件下先训练收敛。
- 再加入视觉扰动评估。

建议先在 PickCube 上完成 RGBD policy，再迁移 Peg。直接在 Peg 上从零做视觉策略风险较高。

### 3.4 推荐工作

#### 工作 A：批量评估脚本

基于 `evaluate_checkpoint.py` 写一个批处理脚本，例如：

```text
benchmark_policies.py
```

功能建议：

- 输入多个 checkpoint。
- 自动跑 PickCube/Peg 的标准评估。
- 输出统一 CSV：
  - task
  - checkpoint
  - episodes
  - success_rate
  - mean_reward
  - mean_steps
  - action_horizon
  - inference_steps
  - max_steps

输出建议：

```text
benchmark_results/benchmark_summary.csv
benchmark_results/benchmark_summary.md
```

对于视觉扰动，还应额外记录：

- perturbation_type
- perturbation_level
- camera_config
- lighting_config
- texture/background_config
- policy_input_type，必须区分 `state` 和 `rgbd`

#### 工作 B：视觉扰动视频采样

当前 `record_policy_video.py` 默认可以录成功视频。任务四建议扩展为：

- 保存失败视频。
- 保存成功视频。
- 按 seed 命名。
- 记录每个视频的 success、steps、reward。
- 记录是否开启视觉扰动。
- 记录扰动类型和强度。

建议输出：

```text
output_peg_analysis/videos/success_seed_*.mp4
output_peg_analysis/videos/fail_seed_*.mp4
output_peg_analysis/video_index.csv
```

#### 工作 C：视觉扰动配置记录

建议每次扰动实验保存配置，例如：

```text
output_visual_perturbation/configs/lighting_low.json
output_visual_perturbation/configs/camera_shift_01.json
output_visual_perturbation/configs/background_color_01.json
```

每个配置至少记录：

- 环境名。
- policy checkpoint。
- policy 输入类型：`state` 或 `rgbd`。
- 扰动类型。
- 扰动参数。
- eval episodes。
- success rate。
- 视频路径。

#### 工作 D：动作统计

对成功和失败 episode 做动作统计：

- 每一维 action 的均值、方差、最大绝对值。
- 动作是否频繁贴边 `-1` 或 `1`。
- 最后 50 步动作幅度。
- 成功/失败 episode 的步数分布。

建议输出：

```text
output_peg_analysis/action_stats.csv
output_peg_analysis/action_stats.png
```

#### 工作 E：曲线汇总

把以下曲线整理到一个报告：

- PickCube final。
- Peg first round。
- Peg resume baseline。
- Peg combined failed attempt。
- 任务三新增实验。

建议输出：

```text
benchmark_results/training_curves_compare.png
benchmark_results/task4_evaluation_report.md
```

### 3.5 任务四验收标准

最低交付：

1. 可以一键复评 PickCube 与 Peg baseline。
2. 有视觉扰动配置文件或配置表。
3. 有统一 CSV 汇总，明确 `policy_input_type=state/rgbd`。
4. 有 PickCube 扰动前后视频。
5. 如果包含 Peg，必须标注为预实验，并说明 Peg 未稳定收敛。

推荐交付：

1. 支持比较任务三不同数据规模模型。
2. 支持自动生成 Markdown benchmark 报告。
3. 如果完成 RGBD policy，给出真正视觉输入下的扰动鲁棒性结论。
4. 如果没有完成 RGBD policy，报告必须明确“当前视觉扰动不影响 state policy 决策输入”。

## 4. 任务五：最终集成、文档、交付与展示

### 4.1 目标

任务五建议负责最终项目整合。它不只是“写报告”，还要保证研究人员可以拿到项目后一键理解：

- 数据在哪里。
- 模型在哪里。
- 怎么复现。
- 哪个结果可信。
- 哪些尝试失败了。
- 后续应该从哪里继续。

### 4.2 输入

任务五应收集：

```text
README.md
任务2_Diffusion_Policy训练技术记录.md
任务3_4_5成员分工与指导.md
output_pickcube_state_final/
output_peg_state_motionplanning_resume/
output_peg_state_combined/
logs/
benchmark_results/             # 如果任务四完成
output_peg_task3_<name>/        # 如果任务三完成
```

### 4.3 推荐工作

#### 工作 A：整理最终目录

建议保留清晰目录，不要删除原始实验。可以新增：

```text
final_artifacts/
  pickcube/
    policy_best.pt
    metrics.csv
    training_curves.png
    pickcube_baseline.mp4
  peg/
    policy_best.pt
    metrics.csv
    training_curves.png
    peg_baseline.mp4
    eval_confirm.csv
  reports/
    README.md
    task2_technical_record.md
    benchmark_report.md
```

注意：复制文件即可，不要移动原目录，避免破坏复现实验路径。

#### 工作 B：最终报告

最终报告建议至少包含：

1. 项目目标。
2. 环境说明。
3. 数据来源与转换。
4. Diffusion Policy 方法说明。
5. PickCube 训练结果。
6. Peg 训练结果与未稳定收敛原因。
7. 任务三改进结果。
8. 任务四评估结果。
9. 视频与曲线证据。
10. 后续工作建议。

必须如实写：

- PickCube 已稳定收敛。
- Peg 当前如果没有进一步提升，只能写“baseline 跑通，确认成功率约 30%”，不能写“稳定收敛”。

#### 工作 C：最终演示材料

建议准备：

```text
final_artifacts/videos/pickcube_baseline.mp4
final_artifacts/videos/peg_baseline.mp4
final_artifacts/figures/pickcube_training_curves.png
final_artifacts/figures/peg_training_curves.png
```

如果任务三提升了 Peg，还应加入：

```text
final_artifacts/videos/peg_improved.mp4
final_artifacts/figures/peg_baseline_vs_improved.png
```

#### 工作 D：备份

```text
D:\备份\Second_State_Diffusion_Policy_FINAL_<日期时间>
```

备份规则：

- 保留数据、输出、日志、文档、视频、checkpoint。
- 可以排除 `.mambaenv`、`.venv`、`.git`、`__pycache__`。

### 4.4 任务五验收标准

最低交付：

1. README 完整可复现。
2. final_artifacts 或等价目录整理完毕。
3. PickCube/Peg 视频可播放。
4. 所有关键 checkpoint 和 CSV 路径清楚。
5. 最终报告不夸大 Peg 结果。

推荐交付：

1. 有一页结果总表。
2. 有一页方法流程图。
3. 有一页失败分析。
4. 有最终备份路径。

## 5. 三个任务之间的协作方式

建议分工：

| 成员 | 主责 | 依赖 | 输出给谁 |
|---|---|---|---|
| 任务三成员 | Peg 策略改进、补数据、训练新模型 | 任务2 baseline | 输出新 checkpoint 和评估结果给任务四 |
| 任务四成员 | 统一评估、视频采样、失败分析 | 任务2 baseline + 任务三新模型 | 输出 benchmark 报告给任务五 |
| 任务五成员 | 最终整合、报告、展示、备份 | 任务二/三/四全部产物 | 输出最终交付包 |

协作建议：

1. 任务三每完成一个实验，就把输出目录、训练命令、数据路径、最好成功率写到一个简短记录里。
2. 任务四不要等待所有任务三实验完成；可以先把任务2 baseline 的评估管线搭起来。
3. 任务五不要只在最后介入；应提前规定最终目录结构和报告模板。
4. 所有人统一使用 `evaluate_checkpoint.py` 的结果作为成功率口径，避免每个人用不同 seed 和 max steps 得出不可比结果。

## 6. 命名规范

新实验输出目录建议：

```text
output_<task>_<method>_<date_or_name>
```

例子：

```text
output_peg_task3_more_mp_20260420
output_peg_task3_unet_v1
output_peg_task3_action_smoothing
```

每个实验目录至少应包含：

```text
metrics.csv
training_curves.png
checkpoints/policy_best.pt
checkpoints/policy_last.pt
eval_confirm_30ep.csv
videos/
```

日志命名：

```text
logs/peg_task3_<method>.log
logs/benchmark_<date>.log
logs/final_packaging_<date>.log
```

## 7. 当前风险清单

1. Peg 未稳定收敛：这是最大风险，后续汇报必须诚实描述。
2. combined 数据已验证变差：不要把它当成默认增强数据。
3. Windows 环境下渲染和 OpenMP 容易出现问题：运行前设置环境变量。
4. GPU 占用不一定持续很高：ManiSkill 环境 step 和 replay 有 CPU 瓶颈。
5. 小样本评估波动大：Peg 至少使用 30 episode 确认。

## 8. 推荐最终结果表模板

后续报告可以使用下面表格：

| 任务 | 方法 | 数据 | checkpoint | episodes | success | mean reward | mean steps | 视频 |
|---|---|---|---|---:|---:|---:|---:|---|
| PickCube | DP baseline | motionplanning 100 | `output_pickcube_state_final/checkpoints/policy_best.pt` | 10 | 100% | 20.41 | 66.2 | `output_pickcube_state_final/videos/pickcube_baseline.mp4` |
| Peg | DP baseline | motionplanning 165 | `output_peg_state_motionplanning_resume/checkpoints/policy_best.pt` | 30 | 30% | 99.19 | 290.4 | `output_peg_state_motionplanning_resume/videos/peg_baseline.mp4` |
| Peg | Task3 improved | 待填写 | 待填写 | 30/50 | 待填写 | 待填写 | 待填写 | 待填写 |

## 9. 交接结论

任务三的重点是提升 Peg；任务四的重点是证明提升是否真实；任务五的重点是把真实结果整理成可复现、可展示、可交付的最终项目。

当前项目已经有足够清楚的 baseline，不需要从零开始。后续成员应在此基础上继续推进，而不是重复环境搭建和 PickCube 训练。
